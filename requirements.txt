matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0

# LLM backends — install whichever you want to run against.
# With none installed (or none configured) the pipeline runs in MOCK_MODE.
google-genai>=1.0.0   # Gemini: Vertex AI or the Gemini API
anthropic>=0.34.0     # Anthropic API (Claude)

# Optional tracing: every agent call shows up in LangSmith when LANGSMITH_TRACING=true.
langsmith>=0.1.0
