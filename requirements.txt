matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0

# LLM backends — install whichever you want to run against.
# With neither installed (or neither configured) the pipeline runs in MOCK_MODE.
google-genai>=1.0.0   # Vertex AI (Gemini)
anthropic>=0.34.0     # Anthropic API (Claude)
